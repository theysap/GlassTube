## What & why

## Checklist

- [ ] Version bumped with `npm run bump -- x.y.z` and a `CHANGELOG.md` entry added
- [ ] Commit subjects follow `vX.Y.Z: summary`
- [ ] `npm run check` passes; changed pages tried with `npm run smoke`
